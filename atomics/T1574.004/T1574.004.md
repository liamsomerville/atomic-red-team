# T1574.004 - Hijack Execution Flow: Dylib Hijacking

## Description from ATT&CK

> Adversaries may execute their own payloads by placing a malicious dynamic library (dylib) with an expected name in a path a victim application searches at runtime. The dynamic loader will try to find the dylibs based on the sequential order of the search paths. Paths to dylibs may be prefixed with `@rpath`, which allows developers to use relative paths to specify an array of search paths used at runtime based on the location of the executable.  Additionally, if weak linking is used, such as the `LC_LOAD_WEAK_DYLIB` function, an application will still execute even if an expected dylib is not present. Weak linking enables developers to run an application on multiple macOS versions as new APIs are added.
> 
> Adversaries may gain execution by inserting malicious dylibs with the name of the missing dylib in the identified path.(Citation: Wardle Dylib Hijack Vulnerable Apps)(Citation: Wardle Dylib Hijacking OSX 2015)(Citation: Github EmpireProject HijackScanner)(Citation: Github EmpireProject CreateHijacker Dylib) Dylibs are loaded into an application's address space allowing the malicious dylib to inherit the application's privilege level and resources. Based on the application, this could result in privilege escalation and uninhibited network access. This method may also evade detection from security products since the execution is masked under a legitimate process.(Citation: Writing Bad Malware for OSX)(Citation: wardle artofmalware volume1)(Citation: MalwareUnicorn macOS Dylib Injection MachO)

[Source](https://attack.mitre.org/techniques/T1574/004)

## Atomic Tests

- [Atomic Test #1: Dylib Hijacking via Writable @rpath](#atomic-test-1-dylib-hijacking-via-writable-rpath)

### Atomic Test #1: Dylib Hijacking via Writable @rpath

Builds a small "victim" binary that is linked against a dylib via `@rpath`, with an `LC_RPATH`
pointing at a writable directory (a stand-in for a real application shipping a dylib dependency
outside its bundle's protected `Frameworks` folder, as documented by Patrick Wardle). The attack
step then overwrites the dylib at that writable path with a malicious version exporting the same
symbols, so that the next time the victim binary runs, `dyld` loads and executes the attacker's
code with the victim process's privileges instead of the legitimate library.

Reference: https://www.virusbulletin.com/uploads/pdf/magazine/2015/vb201503-dylib-hijacking.pdf

**Supported Platforms:** macOS

**auto_generated_guid:** `d99313d5-b4d3-488a-bf34-34cd7d8ac7af`

#### Inputs

| Name | Description | Type | Default Value |
|------|-------------|------|---------------|
| work_dir | Working directory used to build and host the victim binary and dylib. | path | /tmp/T1574.004|
| legit_dylib_source | Source for the legitimate dylib the victim binary is linked against. | path | PathToAtomicsFolder/T1574.004/src/MacOS/legit.c|
| victim_source | Source for the victim binary that loads the dylib via @rpath. | path | PathToAtomicsFolder/T1574.004/src/MacOS/victim.c|
| evil_dylib_source | Source for the malicious dylib that hijacks the victim's dylib load. | path | PathToAtomicsFolder/T1574.004/src/MacOS/evil.c|

#### Attack Commands: Run with `bash`!

```bash
gcc -dynamiclib #{evil_dylib_source} -o #{work_dir}/libhijack.dylib -install_name @rpath/libhijack.dylib
#{work_dir}/victim
cat /tmp/T1574.004_hijacked.txt
```

#### Cleanup Commands

```bash
rm -rf #{work_dir}
rm -f /tmp/T1574.004_hijacked.txt
```

#### Dependencies: Run with `bash`!

##### Description: The victim binary and its legitimate dylib dependency must be built at #{work_dir}

###### Check Prereq Commands

```bash
if [ -f #{work_dir}/victim ] && [ -f #{work_dir}/libhijack.dylib ]; then exit 0; else exit 1; fi
```

###### Get Prereq Commands

```bash
mkdir -p #{work_dir}
gcc -dynamiclib #{legit_dylib_source} -o #{work_dir}/libhijack.dylib -install_name @rpath/libhijack.dylib
gcc #{victim_source} -o #{work_dir}/victim -L#{work_dir} -lhijack -Wl,-rpath,#{work_dir}
```
